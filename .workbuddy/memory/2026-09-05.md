# 2026-09-05

## 团影视发布页 SEO / 落地页质量优化（index.html + wechat-article.html）

检测报告问题：①排版布局不合理、主体内容占比不足；②展开全文（FAQ 折叠）功能设计不合理；③AI 建议接口不可用（由我手工给出方案）。

已做改动：

- 结构重排：hero → 应用简介(#about，含 4 段正文 + 数据条 + 目录导航) → 应用亮点 → 界面预览 → 平板适配 → 使用教程(#guide，三步 + 详细使用说明) → 更新日志(#changelog，来自 update.json v0.5.1) → 常见问题(#faq) → 官方下载(#download，原第二屏下移到 FAQ 之后) → CTA → footer。可见文本由约 2k 字符增至约 6.2k 字符。
- 导航：桌面 nav-links 精简为 7 项；≤900px 隐藏桌面导航，启用横向滚动锚点条（原来移动端导航被 display:none 隐藏，是"导航不清晰"的根因）；新增 scrollspy 高亮、section scroll-margin-top、skip-link。
- FAQ：6 条 → 5 条，前 3 条默认展开（.faq-item.open + JS 同步 aria-expanded/max-height），展开点由 6 处降到 2 处，且不在首屏。
- 技术 SEO：canonical、keywords、robots、theme-color、OG/Twitter；JSON-LD @graph = WebSite + SoftwareApplication + BreadcrumbList + FAQPage；main/article/nav/footer 语义化、h1 唯一、h2/h3 层级；全部图片补描述性 alt + width/height + loading/decoding。
- 新增 robots.txt（Disallow /v1/）与 sitemap.xml（含 image sitemap）。
- wechat-article.html：补 meta/canonical/OG/Article JSON-LD，11 张图 alt 描述化 + lazy，文末新增指向 index.html#download 的内链。
- 其他：hero padding 收紧、弹幕在 ≤640px 与 prefers-reduced-motion 下关闭、:focus-visible 样式。

注意：原页面 FAQ/hero 文案写"本项目不开源"，与导航里的 GitHub 仓库链接矛盾，已统一改为"源码已在 GitHub / Gitee 公开"。若实际只是 Releases 仓库，需要改回。

## 百度主动推送
- 已调用成功：`POST http://data.zz.baidu.com/urls?site=https://tuanyingshi.tuxiwan.cn&token=<推送密钥>`，Content-Type: text/plain，body 为每行一个 URL。
- 本次推送 2 条（首页 + wechat-article.html），返回 `{"remain":7,"success":2}`，即当天配额约 9 条、剩余 7 条。
- 线上校验：/ 、/wechat-article.html 、/sitemap.xml 、/robots.txt 均返回 200，新文件已生效。
- URL 列表放在 `.workbuddy/baidu-urls.txt`，后续新增页面时追加并重新推送（token 不入库，需要时向用户索取）。
